class Hand:
class finger
    def __init__(self):







 class car
     def __init__(self, model: str, type:str, housepower:int)->None:
         self.__model=model
         self.__engine+ Engine(type,housepower)
         self.tire:List[tire]=None*4

        def addTire(self, pos:int, tire:Tire)->None:
            if tire.size
            self.__tire[pos]=tire

     def __str__(self)=str:
            tires =''
            for tire in self.__tires:
                tires +=''+str(tire)

                return "Model: {0},engine:[1], tires:{2} ".format(self.__model, self.__engine,tires)
def tires(self):  #it is getter
    return self.__tires #it is not safe to return the list because it is mutable


         def main():
             t =Tire('snow','27')
             print(t)

         c= Car('Model-S', 'V8',300)
         c.addTire(0,Tire('Snow', 27))
         c.addTire(0, Tire('Snow', 27))
         c.addTire(0, Tire('Snow', 27))
         c.addTire(0, Tire('Snow', 27))


     print (c.tires)
    Print(c.color)
    print(c.__tires)
    tires = c.tires
    tires[0]= Tire('sport', 40)
    print (c)

